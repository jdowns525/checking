<h1>All Posts</h1>

<% @posts.each do |post| %>
  <div>
    <h3><%= post.title %></h3>
    <p><%= post.description %></p>
    <p><%= link_to 'Show', post_path(post) %></p>
  </div>
<% end %>

<%= link_to 'New Post', new_post_path %>

